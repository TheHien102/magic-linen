import styled from 'styled-components';
import tw from 'twin.macro';

export const Login = styled.div`
  ${tw`flex flex-col justify-center items-center  h-[80vh]`}
`;

export const Flex = styled.div`
  ${tw`flex items-center justify-center w-full mt-10`}
`;

export const Wrap = styled.div`
  ${tw`flex flex-col gap-3 items-center justify-center`}
`;

export const Title = styled.h1`
  ${tw`text-center text-black-light `}
`;
